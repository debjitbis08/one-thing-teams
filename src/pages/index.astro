---
// Redirect to app or login based on session
export const prerender = false;

const session = Astro.locals.session;
if (session) {
  return Astro.redirect("/app");
} else {
  return Astro.redirect("/login");
}
---
