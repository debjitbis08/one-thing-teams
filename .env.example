# Database connection string for migrations and runtime
# DATABASE_URL=postgres://username:password@localhost:5432/onething

# Optional overrides for migration script interaction
# CONFIRM_MIGRATIONS=true
